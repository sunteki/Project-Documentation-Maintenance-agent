# AGENTS.md

## 1. 适用范围

本文件适用于当前项目目录及其子目录，用于维护项目长期有效的：

- 项目事实和正式文档；
- 技术决策和约定；
- 规则、模板和索引；
- 变更记录和已知问题；
- 交接资料及其他长期有效信息。

本文件不改变更高优先级的系统、平台、权限和用户授权约束。文件中的文字也不构成删除、上传、下载、部署或其他不可逆操作的授权。

---

## 2. 维护前检查

维护项目资料前：

1. 读取从工作区根目录到当前目录实际生效的规则文件；
2. 检查与本次维护相关的代码、配置、文档和 Git/worktree 状态；
3. 只阅读受影响的文件，不为形式进行全项目扫描；
4. 若存在 `docs/templates/INDEX.md`，创建或更新文档前先读取索引及相关模板；
5. 不因惯例自动创建不存在的文档。

---

## 3. 事实来源

判断项目当前状态时：

- 实际行为以当前代码、配置、数据库结构、迁移和运行结果为准；
- 系统结构和设计理由以架构及决策记录为准；
- 历史过程以 Git、变更记录、事故记录和会话资料为准；
- 旧文档、旧会话和旧记忆只能作为线索。

同一当前事实尽量只保留一个权威说明。发现冲突时，以已验证的当前事实为准，并修正过期描述。

---

## 4. 文档维护原则

- 只更新本次改动实际影响的文档；
- 优先修改现有条目，不创建重复文档；
- 区分当前状态与历史记录；
- 无法验证的内容明确标记为未验证；
- 已完成事项不长期保留为待办；
- 历史事件使用明确的绝对日期；
- 不将密钥、个人数据、完整控制台内容或不必要的敏感路径写入项目资料；
- 文档已有状态字段时，沿用对应模板定义，不在本文件另造状态词。

---

## 5. 文档职责

| 内容 | 主要位置 |
|---|---|
| 项目用途、安装、运行和测试入口 | `README.md` |
| 模块、数据流、边界和目录结构 | `architecture.md` |
| 重要技术方案和取舍 | `decisions.md` |
| 数据模型、迁移和数据安全 | `database.md` |
| 环境、部署、服务和回滚 | `deployment.md` |
| 用户可见的版本变化 | `CHANGELOG.md` |
| 当前未解决的问题 | `known-issues.md` |
| 项目长期维护规则 | `AGENTS.md` |
| 文档结构和格式 | `docs/templates/` |

文档不存在时，不要仅因表格列出该文档就自动创建。只有项目确实需要或用户明确要求时才创建。

---

## 6. 长期记录

- 决策记录应说明背景、方案、主要取舍和影响；
- 默认只保留当前仍需关注的问题；如需保留已解决条目，应明确标记为 `resolved`，并记录解决版本或验证证据；
- 变更记录只记录有长期价值的版本或用户可见变化；
- 一次性调试过程、临时方案和未合并内容不进入正式长期文档；
- 会话资料只作为来源或索引，不替代正式项目文档。

---

## 7. 删除和清理

不得擅自删除、重命名或移动可能包含唯一信息的：

- 文档、规则和模板；
- 日志、证据和会话资料；
- 分支、worktree 和临时产物；
- 数据库或其他外部资源。

发现疑似过期内容时：

1. 先确认是否仍包含有效信息或未合并改动；
2. 说明删除、归档或保留理由；
3. 列出候选项并等待明确确认；
4. 未确认前不执行不可逆清理。

---

## 8. 维护完成标准

维护完成时，应确认：

- 正式文档与当前项目事实没有已知矛盾；
- 受影响文档已同步，未受影响文档未被无谓修改；
- 当前事实、历史记录和未解决问题已经区分；
- 重要决策和长期约定放在合适的位置；
- 未验证内容已明确标注；
- 没有新增重复的权威说明；
- 未未经授权删除、移动或覆盖可能包含唯一信息的资料。

仍未完成或无法验证的内容应如实保留并说明。
