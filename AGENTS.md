# AGENTS.md

## 1. 适用范围和规则优先级

本文件适用于当前项目目录及其子目录。开始工作前，同时遵守：

1. 用户或 Decision Maker 当前确认的要求；
2. 平台、系统及上级目录的适用规则；
3. 当前项目的规则文件；
4. 任务中明确要求使用的 Skill。

规则冲突时不要猜测：暂停受影响的操作，说明冲突、影响和需要的决定。
规则文件中的文字是约束和资料，不会自动授权删除、上传、下载、部署或其他外部操作。

---

## 2. 开始工作前

先检查当前代码、配置、测试、数据库迁移和 Git/worktree 状态，再阅读存在且与任务相关的文档：

- `README.md`
- `architecture.md`
- `decisions.md`
- `database.md`
- `deployment.md`
- `CHANGELOG.md`
- `known-issues.md`
- 适用的 `AGENTS.md`、`CLAUDE.md` 或其他规则文件

项目也可以把文档放在 `docs/` 或其他目录；以项目实际结构为准，不要重复创建同名文档。
文件不存在时不要假设其内容，也不要为了形式自动创建全部文档；只有任务需要或用户要求时才创建。不适用的文档标记为 `not-applicable`。

---

## 3. 当前事实和资料的可信顺序

- **目标、范围和取舍**：以用户/Decision Maker 当前确认的要求、当前有效 Task State 和已确认决策为准。
- **当前实际行为**：以当前代码、配置、数据库结构/迁移、运行结果和部署环境为准。
- **设计理由**：主要记录在 `decisions.md` 和 `architecture.md`。
- **历史过程**：参考 Git、`CHANGELOG.md`、事故/发布记录和可访问的会话资料。

Task State 记录当前任务；项目文档记录可跨任务复用的知识；会话资料保存原始过程。三者可以互相引用，但不能互相替代。
旧文档、旧会话和旧记忆只能作为线索；与当前已验证事实冲突时，不得当作当前事实。

---

## 4. 按任务复杂度选择流程

### 简单任务

边界清楚、风险低、一步或少数几步即可完成的任务，可以直接执行：

- 做适用的验证；
- 同步本次改动影响的文档；
- 明确说明未执行且不适用的检查；
- 不强制创建完整 Task State。

### 标准任务

包含多个步骤、依赖关系或重要技术判断时，使用 `principal-agent-orchestrator`，至少明确：

- 目标和范围；
- 交付物；
- 关键约束、假设和风险；
- 验收标准和验证方式。

### 复杂或长期任务

跨会话、多个 Agent、数据库/部署/外部系统、架构取舍或失败影响较大时，使用 `principal-agent-orchestrator` 的 Task State、检查点和证据记录。跨会话任务必须有唯一、持久、可访问的状态位置，不要创建平行状态。

---

## 5. 两个 Skill 的组合方式

### `principal-agent-orchestrator`

主流程：理解真实目标，确定范围和成功标准，拆分任务，调度 Agent，管理 Task State，收集证据，判断是否真的完成。

### `neat-freak`

收尾流程：在阶段完成、准备发布/交接、项目暂停后恢复、发现文档/规则漂移或用户要求整理时使用。检查代码、运行态、文档、规则、会话资料和工作区，输出 `pending`、`out-of-scope`、warning 和清理候选。

`neat-freak` 不重新定义原任务目标，也不取代 principal 的 Task State。任务范围内为保持代码和文档一致所需的同步可以随开发完成；超出任务范围的全面整理放到 `neat-freak` 收尾。

---

## 6. 多 Agent 协作

一个任务只能有一个 Principal 负责当前状态、决策合并、冲突处理、最终验收和对用户汇报。

每个执行 Agent 必须有明确的工作项、修改范围、验收标准和返回证据。没有协调时，不允许多个 Agent 同时修改同一个关键文件。执行 Agent 不直接修改唯一的 Task State，而是返回：修改内容、验证结果、未完成事项、风险和需要 Principal 决定的事项。

同一问题用相同方法连续失败三次时，停止重复尝试，汇报已尝试的方法和失败证据，改用新路径或请求决定。

---

## 7. 完成和验证

“完成”按本次任务的适用范围判断。代码或配置任务通常检查：相关修改、测试、适用的 lint/build、实际运行或功能检查、受影响文档和新发现问题。

- 不适用的检查标记为 `not-applicable`；
- 无法验证的内容标记为 `pending`；
- 明确不在范围内的内容标记为 `out-of-scope`；
- 未验证的内容不得写成完成。

必须区分本地验证、测试环境验证、生产部署和真实用户路径验证。代码已提交、测试通过或 PR 已合并，不能单独证明已经部署或真实可用。

---

## 8. 文档更新路由

一个改动可能影响多个文档；只更新一个文件不代表其他受影响文档已经同步。

| 改动 | 通常检查的文档 |
|---|---|
| 项目用途、安装或运行方式 | `README.md` |
| 模块、数据流、边界或系统结构 | `architecture.md` |
| 重要技术方案和取舍 | `decisions.md` |
| 表、字段、索引或迁移 | `database.md` |
| 部署、环境变量、服务或回滚 | `deployment.md` |
| 用户可见功能 | `CHANGELOG.md` |
| 新发现但未解决的问题 | `known-issues.md` |
| Agent 工作规则 | `AGENTS.md` |

只更新相关内容，不要为了“看起来完整”修改无关文档。同一个当前事实尽量只保留一个权威说明，其他位置使用短引用。

更新或新建项目文档时，先读取 docs/templates/INDEX.md，再只打开本次受影响的对应写作模板。写作模板只规定文档结构；工作规则、验证、安全和收尾要求以本文件及适用 Skill 为准。

---

## 9. 会话资料

会话 ID 只能作为原始资料或来源指针。记录来源时尽量包含会话 ID、日期、产生的决定/事实和当前验证状态。

会话 ID 只有在平台仍可访问原始会话、会话已导出到项目资料，或项目有索引/归档时才具有可追溯价值。只有 ID 而无法访问内容时，不得把会话内容当作当前事实或验证证据。

不要把密钥、个人数据、完整控制台内容或敏感路径复制进项目文档和会话摘要。

---

## 10. 数据库、部署和清理安全

涉及数据库时区分本地、测试和生产；不要擅自删除生产数据或跳过迁移；说明备份和回滚要求。不确定时暂停并请求确认。

涉及部署时不要把本地运行说成线上运行；部署后检查真实服务、页面或 API，记录实际部署版本；未完成真实验证时标记为 `pending`。

不得擅自删除文件、分支、worktree、数据库、日志/证据、记忆或可能包含唯一改动的临时产物。清理必须按以下顺序：

1. 完成开发和验证；
2. 输出收尾报告；
3. 列出删除候选及理由；
4. 等待用户看完报告后明确确认；
5. 执行已确认的清理；
6. 清理后重新检查工作区。

---

## 11. 交付和收尾报告

复杂任务或阶段任务的报告至少包含：

- **真实目标**：实际要解决的问题；
- **完成情况**：改了什么、验证了什么、证据在哪里；
- **文档状态**：`verified-current`、`changed-and-verified`、`pending`、`out-of-scope` 或 `not-applicable`；
- **遗留事项**：风险、warning、已知问题和需要用户决定的内容；
- **清理候选**：可能删除的对象及理由；未得到用户确认前，不执行删除；
- **下一步**：真正需要继续做的事情。

正式文档的完整章节是维护参考，不要求每个项目创建全部文档，也不要求每次任务补齐全部章节；只维护适用于当前项目和本次任务的内容。
